<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:converter="clr-namespace:Temperature_Converter.Converters"
                    >
    <converter:ImagePathConverter x:Key="ImagePathConverter"/>
    <DataTemplate x:Key="Celsius:Detail">
        <GroupBox Grid.Column="2" Grid.Row="0">
            <GroupBox.Header>
                <TextBlock Text="Celsius" FontWeight="Bold"></TextBlock>
            </GroupBox.Header>
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition  Width="160"></ColumnDefinition>
                </Grid.ColumnDefinitions> 
                <Grid.RowDefinitions> 
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="10"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition> 
                    <RowDefinition Height="10"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <TextBox x:Name="From" Margin="10,10,0,0" FontSize="14" Grid.Column="2" Grid.Row="0" Text="{Binding Degrees, UpdateSourceTrigger=PropertyChanged}" Width="100"></TextBox>
                <Image Height="100" Width="100" Grid.Column="0" HorizontalAlignment="Center" Grid.Row="2" >
                    <Image.Source>
                        <BitmapImage  UriSource="{Binding Converter={StaticResource ImagePathConverter},ConverterParameter=Celsius.png}"  />
                    </Image.Source>
                </Image>
            </Grid>
        </GroupBox>
    </DataTemplate>

</ResourceDictionary>